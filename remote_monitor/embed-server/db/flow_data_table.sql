CREATE TABLE flow_data_logs (
    uid   BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ts    TIMESTAMPTZ NOT NULL,
    wake_count INT NOT NULL,
    avg_flow_rate_lpm DOUBLE PRECISION NOT NULL,
    total_volumn_l DOUBLE PRECISION NOT NULL
);